services:
  - type: web
    name: curso-management
    env: php
    branch: main
    buildCommand: |
      composer install --prefer-dist --no-dev -o
      php artisan key:generate --force
      php artisan storage:link
      php artisan migrate --force
    startCommand: php artisan serve --host 0.0.0.0 --port $PORT
    envVars:
      - key: APP_ENV
        value: production
      - key: APP_DEBUG
        value: "false"
      - key: APP_URL
        value: https://<tu-app>.onrender.com
      - key: DB_CONNECTION
        value: mysql
      - key: DB_HOST  
        fromDatabase:
          name: curso-management-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: curso-management-db
          property: port
      - key: DB_DATABASE
        fromDatabase:
          name: curso-management-db
          property: database
      - key: DB_USERNAME
        fromDatabase:
          name: curso-management-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: curso-management-db
          property: password
      - key: SESSION_DRIVER
        value: database
databases:
  - name: curso-management-db
    engine: mysql
    version: "8.0"
